import { Result } from '../api'
export function ResultView({result,onRestart}:{result:Result;onRestart:()=>void}){return <main className="shell"><div className="eyebrow">Presentation complete</div><h1>Your listening report</h1><section className="stats" aria-label="Results summary"><div><strong>{result.summary.caught}</strong><span>Caught Errors</span></div><div><strong>{result.summary.missed}</strong><span>Missed Errors</span></div><div><strong>{result.summary.false_challenges}</strong><span>False Challenges</span></div></section><h2>Source review</h2><div className="results">{result.errors.map(error=><article key={error.id} className="result-card"><span className={error.caught?'caught':'missed'}>{error.caught?'Caught':'Missed'}</span><h3>What the presenter said</h3><p>{error.presented_statement}</p><h3>What the source supports</h3><p>{error.source_supported_statement}</p><h3>Why it was wrong</h3><p>{error.explanation}</p><details><summary>View source evidence</summary><p>{error.source_evidence}</p></details></article>)}</div><button onClick={onRestart}>Start another presentation</button></main>}
