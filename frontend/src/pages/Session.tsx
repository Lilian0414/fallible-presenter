import { Segment } from '../api';import { ChallengePanel } from '../components/ChallengePanel';import { PresenterCard } from '../components/PresenterCard'
type Feedback={valid:boolean;reasoning_score:number;feedback:string}
export function Session({segment,index,total,challenging,setChallenging,feedback,onChallenge,onContinue,busy,error}:{segment:Segment;index:number;total:number;challenging:boolean;setChallenging:(v:boolean)=>void;feedback?:Feedback;onChallenge:(r:string)=>void;onContinue:()=>void;busy:boolean;error:string}){return <main className="shell session"><header><div className="eyebrow">Fallible Presenter</div><progress value={index+1} max={total}/><span>{index+1} / {total}</span></header><PresenterCard segment={segment} index={index} total={total}/>{error&&<div className="error" role="alert">{error}</div>}{challenging?<><ChallengePanel statement={segment.text} onCancel={()=>setChallenging(false)} onSubmit={onChallenge} busy={busy} feedback={feedback}/>{feedback&&<button onClick={onContinue} disabled={busy}>Continue Presentation</button>}</>:<div className="actions"><button className="secondary" onClick={()=>setChallenging(true)}>Interrupt / Challenge</button><button onClick={onContinue} disabled={busy}>{busy?'Loading…':'Continue'}</button></div>}</main>}
